-- +goose Up
-- +goose StatementBegin
CREATE TABLE IF NOT EXISTS kemono (
    id CHAR(36) NOT NULL,
    image MEDIUMBLOB NOT NULL DEFAULT '',
    prompt TEXT DEFAULT '',
    concepts TEXT DEFAULT '',
    name TEXT DEFAULT '',
    description TEXT DEFAULT '',
    kind INT DEFAULT -1,
    color INT DEFAULT -1,
    is_player BOOLEAN NOT NULL DEFAULT FALSE DEFAULT FALSE,
    is_for_battle BOOLEAN NOT NULL DEFAULT FALSE DEFAULT FALSE,
    is_owned BOOLEAN NOT NULL DEFAULT FALSE DEFAULT FALSE,
    owner_id CHAR(36) DEFAULT '00000000-0000-0000-0000-000000000000',
    is_in_field BOOLEAN NOT NULL DEFAULT TRUE DEFAULT TRUE,
    is_boss BOOLEAN NOT NULL DEFAULT FALSE,
    field INT DEFAULT -1,
    x INT DEFAULT -1,
    y INT DEFAULT -1,
    has_parent BOOLEAN NOT NULL DEFAULT FALSE,
    parent1_id CHAR(36) DEFAULT '00000000-0000-0000-0000-000000000000',
    parent2_id CHAR(36) DEFAULT '00000000-0000-0000-0000-000000000000',
    has_child BOOLEAN NOT NULL DEFAULT FALSE,
    child_id CHAR(36) DEFAULT '00000000-0000-0000-0000-000000000000',
    max_hp INT DEFAULT -1,
    hp INT DEFAULT -1,
    attack INT DEFAULT -1,
    defense INT DEFAULT -1,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id)
);
-- +goose StatementEnd

-- +goose Down
-- +goose StatementBegin
DROP TABLE IF EXISTS kemono;
-- +goose StatementEnd
